if [ $CI_COMMIT_BRANCH == "master" ];
    then TAG="latest";
    else TAG="$CI_COMMIT_BRANCH";
fi

docker pull --name $TAG gcr.io/krossr/client:${TAG}
docker cp gcr.io/krossr/client:${TAG}/node_modules $CI_PROJECT_DIR/node_modules