if [ $CI_COMMIT_BRANCH == "master" ];
    then TAG="latest";
    else TAG="$CI_COMMIT_BRANCH";
fi

docker run -d --name $TAG gcr.io/krossr/client:${TAG}
docker exec $TAG apt-get update && apt-get install npm -y
docker exec $TAG npm run test
docker stop $TAG